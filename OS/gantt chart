/*void gantt(int n,Process p[n]){
    printf("GAntt Chart:\n");
    for (int i = 0; i < n; i++) 
      {
        printf("| P%d\t", p[i].id);
      }
      printf("|\n"); 
     printf("|\n"); printf("%d\t", p[0].at);
      for (int i = 0; i < n; i++) 
      {
        printf("%d\t", p[i].ct);
      }

}
*/